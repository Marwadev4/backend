{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "PYTHON"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --threads 4"
  }
}